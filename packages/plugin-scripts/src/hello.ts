/**
 * Placeholder script to verify the build pipeline.
 * This file can be deleted once real scripts are migrated.
 */

function hello(): string {
  return 'Hello from plugin-scripts!';
}

// When run as a script, output the message
console.log(hello());

export { hello };

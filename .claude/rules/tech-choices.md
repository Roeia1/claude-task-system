# Technical & Technology Choices

- Prefer the **TypeScript LSP tools** (goToDefinition, findReferences, hover, etc.) over native search tools (Grep, Glob) when navigating and understanding TypeScript code.
